import React from 'react';

/**
 * CursorGlow was removed to eliminate interaction jitter,
 * continuous requestAnimationFrame compositing loops, and mousemove re-renders.
 */
export const CursorGlow: React.FC = () => {
  return null;
};

